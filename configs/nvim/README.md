# Neovim config

Plain Neovim configuration using `lazy.nvim` as the plugin manager.

This config does not import LazyVim or another Neovim distribution. Keymaps are
left at Vim/Neovim defaults unless you add your own mappings in
`lua/config/keymaps.lua`.
